# mhd_sql
simple sqlite reader for assembly that takes a file path and a table name  as an argument, and shows a simple "SELECT * FROM ?"  for the table u inserted . 

repo queued after the c compiler 
